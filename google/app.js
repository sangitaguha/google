constData:DataShow;
Bag('http://localhost:5000/getData')
.then(Response=>Response.json())
.then(DataShow=>console.log('data'))

bagButton:("rain") ;
bagText="button"
bagElements=document.getElementById('rain').innerHTML('')
console.log('rain')


constFrom=>Document.getElementById('From');
constNav=>Document.getElementById('nav');
constImage=>Document.getElementById('image');
 
bagDiv(nice)
for (const div in nice ) {
    if (Object.hasOwnProperty.call(object, div)) {
        const element = object[div];
        constElements = Document.getElementById('nice')
        
    }
}
